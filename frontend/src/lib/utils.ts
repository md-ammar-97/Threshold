// shadcn-scaffolded components import `cn` from "@/lib/utils" by convention —
// this re-export keeps that working without editing every generated file,
// while the actual implementation stays in ./utils/cn alongside its README.
export { cn } from "./utils/cn";
